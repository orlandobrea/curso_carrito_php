<?php
defined('BASEPATH') OR exit('No direct script access allowed');
?><!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<title>Pagina de prueba</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>
<body>

<div id="container-fluid">
	<div class="col-xs-10 col-xs-offset-1">
		<div class="row">
			<div class="col-xs-9">
				<h1>Mi Tienda online</h1>
			</div>
			<div class="col-xs-3">
				<a href="{{base_url}}/ver_carrito" class="btn btn-lg">Carrito <i class="glyphicon glyphicon-shopping-cart"></i> <span class="badge">{{carrito.cantidadItems}}</span></a>
			</div>
		</div>
		
		<div class="col-xs-6 col-xs-offset-6">
			<form method="get">
				<span class="col-xs-10">
					<input type="text" name="filtro" class="form-control" value="{{filtro}}" />
				</span>
				<span class="col-xs-2">
					<input type="submit" value="Buscar" class="btn btn-default"/>
				</span>
			</form>
		</div>
		<br>
		<hr>
		
		<div>
			{% for producto in listado %}
			<div class="col-xs-6 col-md-4">
				<img src="http://lorempixel.com/200/200/technics/{{producto.id}}" />
				<div class="col-xs-6">
					{{producto.descripcion}}
					<p class="precio">Precio: {{producto.precio}}</p>
				</div>
				<div class="col-xs-6">
					<a href="carrito/agregar/{{producto.id}}" class="btn btn-success">Agregar</a>
				</div>
			</div>
			{% endfor %}

		</div>

		{% if pagina>1 %}
			<a href="?pagina={{pagina-1}}&filtro={{filtro}}">Anterior</a>
		{% endif %}

		{% if pagina<totalPaginas %}
			<a href="?pagina={{pagina+1}}&filtro={{filtro}}">Siguiente</a>
		{% endif %}
	</div>
</div>

</body>
</html>